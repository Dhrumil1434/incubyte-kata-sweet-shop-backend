#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Ensure dependencies are installed (CI or fresh clone safety)
command -v npm >/dev/null 2>&1 && [ -f package.json ] || exit 1

# Run linters on staged files
npx --no-install lint-staged


